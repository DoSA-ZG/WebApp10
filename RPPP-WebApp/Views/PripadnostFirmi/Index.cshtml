@model RPPP_WebApp.ViewModels.PripadnostiFirmiViewModel
@{
	ViewBag.Title = "Pripadnost Firmi";
}

<h2>Popis pripadnosti firmi</h2>

<pager page-info="@Model.PagingInfo" page-action="Index" page-title="Unesite željenu stranicu"
	   class="float-end">
</pager>

<table class="table table-sm table-striped table-hover">
    <thead>
        <tr>
            <th>
                <a asp-route-sort="1" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 1 ? !Model.PagingInfo.Ascending : true)">
                    Id Firma
                </a>
            </th>
            <th>
                <a asp-route-sort="2" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 2 ? !Model.PagingInfo.Ascending : true)">
                    NazivFirma
                </a>
            </th>
            <th>
                <a asp-route-sort="2" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 3 ? !Model.PagingInfo.Ascending : true)">
                    AdresaSjedista
                </a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var pripadnostFirmi in Model.PripadnostFirmis)
        {
            <tr>
                <td class="text-center">@pripadnostFirmi.IdFirma</td>
                <td class="text-left">@pripadnostFirmi.NazivFirma</td>
                <td class="text-left">@pripadnostFirmi.AdresaSjedista</td>
            </tr>
        }
    </tbody>
</table>

@section scripts {
    <script src="~/js/pager.js" asp-append-version="true"></script>
}